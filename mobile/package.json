{
  "name": "lfg-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "iOS wrapper for the lfg PWA — pairing shell + native voice foundation",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "~5.9.0"
  }
}
