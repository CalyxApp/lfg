# lfg — Calyx mobile app control plane

Production deploy/ops facts + adoption history: `docs/upstream-adoption-plan.md`.


## Cross-workstream tracking (all Calyx/lfg agents)

Before starting work: read `tracking/STATUS.md` in the **PlatosRaveCave** repo
(VPS: `/home/openclaw/repos/PlatosRaveCave/tracking/STATUS.md`) so you know the
state of the other workstreams (lfg mobile app, VS Code extension, Chrome
new-tab). After finishing real work in this repo: update this workstream's file
under `tracking/workstreams/` and its `STATUS.md` block there, then commit
(stage only `tracking/`) and push.
